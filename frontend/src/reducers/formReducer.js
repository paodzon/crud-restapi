const INITIAL_STATE = {
    formList: []
}

const formReducer =(state =INITIAL_STATE, action) =>{
    switch(action.type){
        default:
            return state
    }
}

export default formReducer;